# dependencies
node_modules/

# local reference repos (third-party, ~88MB)
ref/

# local probe scaffolding (contains a real bot token in telegram-test-overlay.yml)
probe/

# internal task handoff document (local paths, machine details)
TASK.md

# handoff ledgers (per-session internal notes with machine details)
handoff-*.md

# other tools' state dirs (Claude/Codex replay, checkpoint, session state)
.omc/

# runtime data
data/

# build artifacts / OS junk
*.tsbuildinfo
.DS_Store
Thumbs.db
